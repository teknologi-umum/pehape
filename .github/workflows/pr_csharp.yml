name: C# PR Check

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: Backend
    runs-on: windows-latest
    env:
      working-directory: ./csharp
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 6.0.x
      - name: Restore dependencies
        run: dotnet restore Pehape.sln
      - name: Build
        run: dotnet build --no-restore --configuration Release Pehape.sln
      - name: Test
        run: dotnet test --no-build --configuration Release --verbosity normal --collect="XPlat Code Coverage" Pehape.sln
      - name: Report coverage
        uses: codecov/codecov-action@v2
        with:
          flags: csharp
